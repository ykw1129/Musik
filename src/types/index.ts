export type User = {
    status: 1
    fileIds: string[]
    friendsIds: string[]
    collections: string[]
    dynamics: string[]
    playlists: string[]
    _id: string
    email: string
    nickName: string
}